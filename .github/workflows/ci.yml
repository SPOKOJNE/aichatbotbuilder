name: CI

on:
  push:
    branches: [develop, test, main]
  workflow_dispatch:

jobs:
  lint:
    name: CI / lint
    runs-on: ubuntu-latest
    steps:
      - run: echo "Linting..."

  build:
    name: CI / build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building..."

  test:
    name: CI / test
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing..."